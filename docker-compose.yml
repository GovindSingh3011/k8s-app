services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: k8s-app:latest
    container_name: k8s-app
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
      - PORT=3000
    restart: unless-stopped
