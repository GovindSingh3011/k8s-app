apiVersion: v1
kind: Service
metadata:
  name: k8s-app-service
  namespace: k8s-app
  labels:
    app: k8s-app
spec:
  type: ClusterIP
  selector:
    app: k8s-app
  ports:
  - name: http
    port: 80
    targetPort: 3000
    